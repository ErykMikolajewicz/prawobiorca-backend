[project]
name = "embedding-service"
version = "0.1.0"
description = "Service to make embeddings for documents."
requires-python = "==3.14.*"
dependencies = [
    "fastapi==0.141.*",
    "granian==2.8.*",
    "onnxruntime==1.29.*",
    "transformers==5.16.*",
]

