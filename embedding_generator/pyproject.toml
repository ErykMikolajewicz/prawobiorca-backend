[project]
name = "embedding_generator"
version = "0.2.0"
description = "Benchmark for various options of deploying embedding models."
requires-python = "==3.13.*"
dependencies = [
    "fastapi==0.135.*",
    "granian==2.7.*",
    "onnxruntime==1.24.*",
    "pydantic-settings==2.13.*",
    "python-multipart==0.0.22",
    "transformers==5.3.*",
    "unstructured[pdf]==0.21.*",
]
